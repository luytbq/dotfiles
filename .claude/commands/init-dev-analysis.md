Copy the template from `~/.claude/assets/dev-analysis-template.md` to the current working directory.

- If `$ARGUMENTS` is provided, use it as the filename (append `.md` if missing).
- Otherwise use `dev-analysis.md`.

Write the file and confirm the path. Do not ask any questions. Do not replace any placeholders.
