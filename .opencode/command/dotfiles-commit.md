---
description: Commit and push dotfiles changes to the repository.
agent: dotfiles
---

When you need to use git commands, replace `git` with `dotfiles` - it is a wrapper script that manages your dotfiles repository.
Commit and Push dotfiles Changes.
